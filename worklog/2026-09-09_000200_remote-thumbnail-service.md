# Remote thumbnail service and remembered fallback

- Time: 2026-09-08 late evening through 2026-09-09 00:02 Asia/Bangkok.
- Request: offload Full Lanczos/Fast thumbnail generation to this Windows machine; independent quality/workers; remembered unavailable state, background recovery, Management status/manual retry. User changed local IIS binding to wildcard and requested retest.
- Source changes: ThumbService project/config/IIS template; main project excludes that subproject; GalleryOptions, RemoteResizer, ThumbnailService, ThumbnailQueueSettings, Program, AdminController, Management view/site.js, default appsettings; tests/RemoteResizerHarness and resizer-management.cjs; durable notes and deployment/THUMB-SERVICE.md.
- Behavior: raw-byte authenticated HTTPS API; bounded service concurrency and separate bounded client priority queue; existing shared cache stripes/source recheck/atomic publication. Offline jobs immediately use local without reconnect attempts. Background health checks run every 30 seconds; manual retry wakes same task. Successful validated health restores service use. Management shows state/check time and refreshes every 3 seconds. Cache suffix changes for remote URL/quality; fallback cache entries remain usable after recovery.
- Validation: main Release build and Windows-IIS publish passed with no build warnings/errors; separate service publish passed. Fake transport harness proved 100 offline jobs make zero resize attempts/extra probes, a failing resize disables later calls, manual retry reconnects and scheduled polling recovers. Actual local IIS HTTPS tests with exact certificate pin passed both full and fast WebP output 240x480 from 600x1200 JPEG, before and after wildcard binding. Unauthenticated service request returned 401. Real headless Edge on isolated local Gallery database passed authenticated Management Connected status, antiforgery retry POST and newer checked timestamp with no browser errors. JavaScript syntax and git diff whitespace checks passed (line-ending notices only). Temporary local fixture app stopped.
- Local deployment: C:\Users\tatsa\web\ThumbService. IIS initially returned 500.19 access denied; granted built-in IIS worker/anonymous identities Read & Execute on this service directory only. API key generated in deployed configuration only, never source. Initial named binding required Host override; after user's wildcard change, direct IP tests passed and NAS override was cleared. Certificate pin remains necessary because certificate is for a DNS name, not IP.
- NAS deployment: Windows NAS only, backups web-data/backups/20260908-235818-index-deploy and 20260909-000046-index-deploy; additional original-config backups before both intentional config edits. Added remote URL/key/pin/quality78/workers4/retry30 to existing config without replacing DB/cache/keys/tools. Application files copy/hash verified. NAS TLS health returned status ok after restart. Local IIS access log proves 192.168.1.29 authenticated health 200 and repeated resize POST 200 (including after final wildcard config update); these are actual NAS-to-service calls, not inferred from build output.
- Remaining boundaries: authenticated UI test was local, not a logged-in production NAS browser. Did not deliberately stop the production service to test failover; outage/recovery logic was tested with deterministic fake transport. No performance comparison/throughput claim. No Linux deployment, C:\Web deployment or Git push. Service limits intentionally fall back locally for over-limit/unsupported files.
