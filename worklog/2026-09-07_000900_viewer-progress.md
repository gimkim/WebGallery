# Full-image progress across all load paths

- Request: show progress while loading full images.
- Changed site.js to show the overlay immediately for cache probing, memory Blob decoding and HTTP cache decoding, as well as network downloads. Show measured percentage and loaded/total bytes when known; indeterminate progress with loaded bytes when total is unknown. Completed transfers stay at 100% with Decoding full image until displayed. Cached paths also show decoding status rather than silently waiting. Common display completion hides the overlay with generation guards; errors stop progress for cached and network paths.
- Added tests/viewer-progress.cjs for numeric, unknown-length and completed transfer states; expanded viewer-placeholder.cjs to assert visible progress in memory/cache/network paths. Both passed; Node syntax and Windows-IIS publish passed. No real-browser throttled end-to-end test this session.
- Backed up NAS app to web-data/backups/20260907-000809-viewer-progress. Deployed only the changed wwwroot/js/site.js; no IIS restart, binaries, database, settings or cache changes. NAS and public JS SHA-256 match source; live health status ok.
