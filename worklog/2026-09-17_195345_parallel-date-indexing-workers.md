# Configurable continuous Date Taken indexing

- Date: 2026-09-17 19:53 Asia/Bangkok. User requests proposed continuous parallel indexing with configurable worker count, default4.
- DateTakenIndexer now has bounded continuous coordinator: at most128 held identities across queued/active/uncommitted results, fetch pages up to64, no per-file20ms delay and no batch-wide read barrier. Fill freed slots promptly; one independent SQLite writer commits up to32 results in a transaction, partial batches flush around500ms. Readers/query/writer each own their scope/DbContext. Exclude held identities until commit, preserving fingerprint/owner/physical-key guards and retry/cancellation. Unsaved results remain pending after restart. Recently viewed folder priority retained. No thumbnail scheduling changes.
- Management adds Date Taken indexing workers (1–16, default4) and separate Save indexing settings form. Admin authorization/antiforgery, serialized save, persist DateTakenWorkers in AppSettings and apply only after DB commit. Startup restores saved setting or defaults4. Limit decrease lets in-flight reads finish, no forced cancellation. No app restart needed when saving.
- Files: DateTakenIndexer, DatabaseInitializer, AdminController, Admin view, AGENTS, DateTakenHarness and indexing-progress-browser test.
- Validation: DateTakenHarness passed default/runtime setting checks and existing EXIF/date sorting/re-index/progress tests;96-image priority-folder workload flushed multiple result batches and a vanished file deferred without blocking others. Real headless Edge/local ASP.NET test saved2 then4 via form and verified values on fresh Management navigation; progress API/UI/auth checks passed. Both publish profiles and git diff --check passed. Tests establish functional completion/bounds by implementation, not a live NAS throughput/saturation benchmark. Local server stopped.
- NAS preserving deployment hash-verified; config retained; backup web-data/backups/20260917-195338-index-deploy. Pinned HTTPS health returned ok. Linux output published only. No cache/index reset, production re-index-all, migrations or Git push.
