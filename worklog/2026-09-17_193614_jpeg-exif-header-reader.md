# Deploy bounded JPEG Date Taken reader

- Date: 2026-09-17 19:36 Asia/Bangkok. User explicitly requests fixing confirmed whole-file JPEG metadata reads and deploying.
- Added Services/JpegDateTaken.cs; DateTakenIndexer sniffs JPEG SOI and uses the dedicated reader. Reads APP1 Exif, seeks other segments, stops before SOS/EOI. Bounded4096 marker segments/padding,4MiB aggregate APP1 bytes, validated lengths against file length. Minimal TIFF parser supports both byte orders, validates offsets/counts and reads DateTimeOriginal through IFD0/Exif IFD only, without allocating from TIFF counts. No original pixel decoding, compressed-data scan or per-file process for JPEG. Cancellation retained. Non-JPEG Identify and RAW ExifTool are unchanged.
- No index/cache/key reset. Existing completed values remain; pending images automatically use new reader. Invalid JPEG structure follows existing retry behavior, absent EXIF remains unknown.
- Validation: JpegHeaderHarness passes LE/BE tags, sentinel-guarded no-image-body reads, missing EXIF, malicious offsets/counts, truncation and cancellation. MetadataReadAudit now calls production reader and asserts date equality against ImageSharp on two real NAS JPEGs:11829322 ->10358 bytes read,1964629 ->13171 bytes read. These are stream-read counts, not uncached physical disk bytes; warmed SMB timings do not establish NAS throughput. DateTakenHarness all sorting/index/progress/re-index tests pass. Build0 warnings/errors, both publish profiles and git diff --check pass. No UI changes or browser test required; overall live NAS indexing speed not benchmarked.
- NAS deployed via preserving helper; hashes verified, config retained, backup web-data/backups/20260917-193607-index-deploy. Host/SNI-pinned health returned ok. Linux publish prepared, no container deployment. Updated AGENTS diagnostic supersession; no Git push.
