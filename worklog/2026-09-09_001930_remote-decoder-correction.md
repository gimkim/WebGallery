# Remote decoder selection correction

- Request: remote quality means Full decode versus Faster JPEG (reduced IDCT), not numeric WebP quality. Corrects the interpretation recorded in 2026-09-09_001400_management-remote-settings.md.
- Changed Models/GalleryOptions.cs, Services/RemoteResizer.cs, Services/ThumbnailService.cs, Data/DatabaseInitializer.cs, Controllers/AdminController.cs, Views/Admin/Index.cshtml, both remote harness tests, AGENTS.md, PROJECT_NOTES and THUMB-SERVICE guide.
- Management now saves validated ResizerServiceDecodeMode (full/jpeg-idct) with workers to SQLite and applies immediately. Config default is full; numeric WebP configuration/legacy value is preserved but removed from UI. Remote mode is independent of local decoding. Cache suffix includes remote mode, jobs capture that mode before enqueue, recognized old-mode URLs keep their mode. Local fallback retains local decoder choice.
- Validation: Release build and Windows-IIS publish succeeded. Isolated headless Edge Management test passed mode selection/save/reload, live status, invalid-mode rejection, antiforgery enforcement, unchanged local worker value, no browser errors. Test restored prior mode/workers and local fixture app was stopped. Harness passed mode-specific cache identity, historical mode resolution and existing unavailable/manual/scheduled recovery checks. No production failover induced.
- Deployment: Windows NAS copy/hash verified, backup web-data/backups/20260909-001902-index-deploy. Existing production config, database, cache, keys and tools preserved. NAS TLS health returned status ok. Authenticated browser testing was local, not production NAS. No separate ThumbService deployment required; existing API already accepts full/fast. No Git push or Linux/local C:\Web update.
