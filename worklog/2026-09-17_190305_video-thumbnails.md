# Video thumbnails

- Date: 2026-09-17 19:03 Asia/Bangkok. Request: generate missing video thumbnails.
- Added VideoThumbnail source adapter: FFmpeg extracts a JPEG at1s, falls back to0s for short clips; preserves display aspect ratio and rotation through FFmpeg defaults, bounds frame1500px,12MiB pipe output,30-second timeout, max2 processes/one decoding thread each. Cancellation kills subprocess tree. No video temp files, no network input protocols or playlist demuxers. Uses existing Gallery:FfmpegPath on Windows and Docker.
- ThumbnailService local and RemoteResizer queues use the adapter inside existing striped jobs. Remote gets frame JPEG, not entire video; frame extraction uses Gallery host CPU even when remote is connected. Extraction errors are422 and do not mark remote offline. Shared cache fingerprints/locks/source rechecks/atomic publication and small-before-medium scheduling retained.
- Index pending/medium/cover/progress queries now include images OR videos. Add video eligibility index and detect classification changes. Fallback scanner/covers and upload completion support videos too. Grid/List renders lazy thumbnails with play overlay, contain styling and existing viewport cancellation/medium upgrade. Image viewer and EXIF endpoints remain image-only.
- Files: new Services/VideoThumbnail.cs; ThumbnailService, RemoteResizer, GalleryIndexService, FileSystemService, BackgroundThumbnailService; Gallery/Files controllers; Gallery/Admin views; site.js/site.css; AGENTS.md. New VideoThumbnailHarness/browser test; extended MediumThumbnailHarness and GalleryIndexHarness.
- Validation: build0 warnings/errors; VideoThumbnailHarness passed concurrent owner dedup, small/medium dimensions/aspect, cache reuse, short portrait clip fallback, corrupt input and cancellation. MediumThumbnailHarness passed remote frame resizing plus existing queue/backpressure/fallback tests. GalleryIndexHarness passed including new video small/medium eligibility, folder covers and Management totals. Headless Edge against isolated local ASP.NET app passed Grid/List video thumbnails, play overlay, contain CSS, WebP endpoint and guest denial. Initial browser test needed explicit root navigation and absolute API URLs; corrected and rerun successfully. Generated two synthetic test videos only under ignored App_Data fixture. Local server stopped.
- Both publish profiles succeeded. NAS deployment backup web-data/backups/20260917-190245-index-deploy; config preserved, copied files hash-verified, existing NAS FFmpeg confirmed present, pinned HTTPS /gallery/health status ok. No authenticated live NAS video viewing, exotic codec/rotation/SAR or Linux runtime test; Linux published only. git diff --check passed. Existing cache/data/media not purged and no Git push.
