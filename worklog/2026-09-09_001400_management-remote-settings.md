# Management remote quality and workers

- Request: edit remote thumbnail quality and workers from Management.
- Changes: AdminController dedicated validated/antiforgery-protected save, Management input form, DatabaseInitializer SQLite override loading, RemoteResizer live quality/queue update, ThumbnailService quality snapshot and recognized old-version handling, ThumbnailQueueSettings live suffix provider, RemoteResizerHarness/resizer-management.cjs, agent notes and deployment guide.
- Persistence: SQLite ResizerServiceQuality (1–100), ResizerServiceWorkers (1–16), config defaults when absent. Serialize remote saves, commit both values before applying runtime changes; failed save leaves runtime unchanged. Local settings and service credentials/config remain untouched. ThumbService hard worker ceiling remains separate and explained in UI.
- Cache/concurrency: quality and cache key captured together before enqueue. Existing immutable URL suffixes map back to their original quality/mode. New quality changes new-page browser/server identities, without deleting old cache. Worker changes update/wake the existing queue; active jobs finish normally. Connection pool supports the full 16-slot adjustable range, with actual job admission bounded by the queue.
- Validation: Release build passed initially without warnings/errors; focused headless Edge on isolated local Gallery passed Connected status/manual retry, quality83/workers3 save and immediate status values, reload persistence, invalid101/0 rejection, missing antiforgery400, unchanged local workers, no browser errors. Test restored original remote values. Regression harness passed live setting update, old URL quality preservation, worker-only changes preserving cache suffix, offline100 jobs/no retries and manual/scheduled recovery. Later build encountered the owned local fixture DLL lock, succeeded after stopping that fixture; Windows-IIS publish then passed. Startup override loading is implemented; browser reload tested, not a dedicated restart-with-different-saved-values case.
- Deploy: Windows NAS application copy/hash verified with backup web-data/backups/20260909-001339-index-deploy. Existing production appsettings/web.config, DB/cache/keys/tools preserved. No ThumbService redeployment necessary; no Linux or C:\Web deployment, no Git push. Authenticated browser validation was local, not production NAS.
