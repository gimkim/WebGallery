# Separate date sorting

- Date: 2026-09-17 18:48 Asia/Bangkok.
- Request: split Date into Date Modified and Date Taken.
- Implementation: preserved date query/cookies/shares as modification time; added taken ascending/descending with unknown capture dates last and folders first. EXIF DateTimeOriginal is camera wall-clock (no guessed timezone/mtime fallback). Images without this tag, videos and folders have unknown capture date.
- Added nullable DateTakenTicks and DateTakenScanAfter to derived index, idempotent additive SQLite upgrade and lookup index. A single background metadata reader identifies image headers; RAW uses bounded 15-second ExifTool tag-only extraction, not demosaic/preview extraction. Initial backfill starts after10 seconds, reads32 records/batch, unknown dates are recorded once; read errors defer1 hour. Source fingerprint refresh invalidates capture metadata. Navigation only reads indexed values. Refresh/revisit to see newly backfilled dates; no live re-sort while viewing.
- Changed Models/GalleryIndexEntry.cs, ViewModels/GalleryViewModels.cs, Services/GalleryIndexService.cs, new Services/DateTakenIndexer.cs, Services/FileSystemService.cs, Program.cs, Controllers/GalleryController.cs, Gallery/Collections Razor labels, toolbar CSS, AGENTS.md and two new test harnesses. Collection folder-only picker retains Date Modified (folders have no EXIF capture date).
- Validation: Release build0 warnings/errors; DateTakenHarness passed JPEG EXIF asc/desc, missing-date placement, independent modified order, selected-share indexed retrieval, idempotent old-schema upgrade, source-change invalidation, malformed date, real Fuji RAW EXIF extraction. Existing GalleryIndexHarness passed. Headless Edge against isolated ASP.NET fixture passed eight dropdown choices, all four date-direction choices through SPA and remembered cookie on fresh navigation. No authenticated production visual check or video capture-date extraction.
- Windows-IIS and Linux-Docker publish passed. Windows NAS deployed using state-preserving helper, config preserved and application hashes verified; backup web-data/backups/20260917-184618-index-deploy. NAS hostname/SNI-pinned /gallery/health returned status ok. Docker output prepared but no container deployed. Local test server stopped. git diff --check passed (existing line-ending warnings only). No thumbnail cache reset or public Git push.
