# Shared quality cache and separate remote background/total limits

- Request: remote/local share cache, separate only Full/Faster JPEG quality; independently configure remote Background thumbnail workers and Concurrent thumbnail jobs from local.
- Files: ThumbnailService, ThumbnailQueueSettings, RemoteResizer, BackgroundThumbnailService, GalleryOptions, DatabaseInitializer, AdminController, Management view, BackgroundThumbnailHarness, resizer-management.cjs, agent notes and deployment guide.
- Cache: canonical contain-v1/contain-idct-v1 with existing source/size/mtime/owner/dimensions/common encoder-quality fingerprint. No URL/backend/unused mode. Remote encoder quality matches local. Captured requested decoder persists through fallback. Existing canonical cache hits reuse untouched files. Compatible same-decoder legacy aliases promote under shared stripe through atomic copy with source recheck; originals remain. Ambiguous mixed-decoder aliases are not trusted.
- Scheduling: remote background 0–16 separately persisted, validated <= remote total1–16. Startup seeds it once from previous background opt-in, independent afterward. Connected scanner uses remote background slots; unavailable scanner uses local slots. Local background0 refuses background fallback instead of parking forever. Mode/limit/availability changes notify scanner to cancel/restart; stale signature feed stops refill and submits captured mode. No old-signature infinite cache-hit loop. Index still stores one ready signature; mode switching can recheck files, not re-encode hits.
- Validation: Release build passed. Background harness passed priority/total/background caps, shared remote/local cache exact-path reuse, no rewriting original Full, different Fast path, offline fallback/local Fast same file, mode switch-back reuse, change notifications, and byte-for-byte legacy promotion preserving original. Continuous feed harness crossed150 items while first remained blocked and passed boundedness/cancellation. Headless Edge isolated local Management passed separate remote fields save/reload, local fields unchanged, background>total rejection, invalid mode, antiforgery, retry/status, no browser errors. Fixture settings restored and server stopped. Production outages were not induced; authenticated browser test was local rather than NAS.
- Deployment: Windows-IIS publish and Windows NAS application update, with application/config/state backup and copy hash checks; persistent DB/cache/keys/tools preserved. No cache purge, no ThumbService binary update, no Linux/local C:\Web deployment or Git push. Final NAS health/backup outcome recorded in the session response.
