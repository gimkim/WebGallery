# Infinite scrolling and sorted folder manifest

- Request: replace visible paging with infinite scrolling, right-side sort timeline/alphabet rail, date month/year headers; compact whole-folder section API (not per-file detail); deploy to Windows NAS.
- Changes: GalleryBrowseSections builds contiguous sorted groups and listing revision. GalleryController exposes authorized no-store BrowseManifest (total, pageSize, revision, sections with offset/count/label). Existing 500-item HTML API remains the bounded transport. Gallery view renders chunks, load edges, date headings and real page/hash jump links. gallery-stream.js appends/prepends without remounting current selection/viewer, binds new thumbnails/videos/write cards, highlights visible rail position, checks revision before appending. Missing jump windows navigate via SPA without downloading preceding pages. Loading chunks show density-matched placeholders; existing SPA jump uses element placeholders. Router preserves target hashes. CSS reserves rail space and supports both Grid/List and mobile widths. AGENTS updated.
- Files: Services/GalleryBrowseSections.cs, Controllers/GalleryController.cs, ViewModels/GalleryViewModels.cs, Views/Gallery/Index.cshtml, Views/Shared/_Layout.cshtml, wwwroot/css/site.css, wwwroot/js/{gallery-stream,site,video-player,file-operations,spa-router}.js, tests/infinite-scroll-browser.cjs, AGENTS.md.
- Validation: Release build and Windows-IIS/Linux-Docker publishes succeeded. Headless Edge against isolated local fixture passed 501-entry infinite append, prepend, preservation/new selection, SPA jump URL/hash, date headers, manifest counts/no item array, anonymous denial, and no page runtime errors. JavaScript syntax and git diff whitespace checks passed. This is a functional 501-item fixture test, not a huge-folder NAS frame-time benchmark.
- Deployment: preserving NAS script copied/hash-verified application to \\Gimkim-nas\c\Users\tatsa\web\imagegallery; appsettings.json/web.config retained, no database/cache reset. Backup: web-data/backups/20260917-212401-index-deploy. SNI-pinned HTTPS /gallery/health returned {"status":"ok"}. Linux publish prepared only. No Git push. Production authenticated browser session not inspected.
- Limits: search/filter/select-all operate on currently loaded items. Reordered/changed listing revisions show refresh error instead of mixing old/new ranges. Jump starts a bounded page window; Load earlier items retrieves prior ranges. Appended DOM remains mounted (no full virtualization); very long sessions may accumulate many cards. Dynamic image/video bindings are implemented; media playback after append and mobile visual review remain manual checks. Existing tests/folder-pagination-browser.cjs describes superseded pagination and is replaced for this feature by infinite-scroll-browser.cjs.
