# Small/medium scheduling, preemption and Management progress

2026-09-09 06:58 Asia/Bangkok. Consolidates the interrupted implementation and subsequent user clarifications.

- Requests: remote-first with local fallback for both sizes; indexed background completes all small work before medium; on-demand small then medium preempts background; medium on-demand always queued regardless of remote availability. Show separate small/medium Management counts with lightweight polling (supersedes prior page-load-only requirement).
- Implementation: additive idempotent MediumThumbnailSignature/MediumRetryAfter schema upgrade; separate medium pending/ready/defer paths with source-fingerprint guards and unchanged cache identities. Background continuous feed first drains small then medium; deferred/failed small blocks medium until resolved. Newly discovered small blocks further medium work. On-demand direct medium ensures small cache first. Medium local decode uses1500x1500 bounds, preserving ratio.
- Queue: bounded six lanes ordered foreground small/medium before background small/medium; cooperative cancellation of running background and shared local/remote foreground reservations. Foreground can displace a queued background item at full capacity. Preemption has a distinct non-failure exception, does not mark remote offline or defer indexed work for an hour. Existing stripes, temp cleanup, atomic publication, configured workers and zero-disable remain. Already-running work may take time to observe cancellation; no unsafe forced thread termination.
- Progress: one Admin-only aggregate endpoint with shared10-second cached results and serialized refresh. Initial server render plus one page-scoped15-second poll for all users; hidden-tab skip, no overlap, last-values-on-error. Separate small/medium ready,total,remaining; small/medium generation records readiness; physical cache deletion remains an index-lag caveat.
- Files: Services/ThumbnailWorkQueue.cs, ThumbnailService.cs, RemoteResizer.cs, BackgroundThumbnailService.cs, GalleryIndexService.cs; Models/GalleryIndexEntry.cs; Controllers/AdminController.cs; ViewModels/GalleryViewModels.cs; Views/Admin/Index.cshtml; wwwroot/js/site.js; tests/MediumThumbnailHarness, GalleryIndexHarness, ThumbnailPriorityHarness, admin-thumbnail-counts.cjs; AGENTS.md/.agents/PROJECT_NOTES.md.
- Validation: Release builds and both Windows-IIS/Linux-Docker publish passed. Medium harness verifies offline local medium, automatic small-first, remote shared-lock dedup,1500px aspect ratio, remote failure fallback, remote background preemption without offline state or leftover temporary files. Index harness verifies legacy-column upgrade/idempotence, background small then medium, deferred-small gate, changed fingerprint guards and existing watcher/ownership behavior. Priority harness verifies preemption, strict foreground ordering, waiting for active foreground small, cross-queue reservation, full-queue displacement. Feed harness continues across150 entries with early job blocked, no duplicate or batch barrier.
- Real local Edge Management test:10 users match indexed small/medium counts, anonymous endpoint rejected, one poll updates all cards, hidden-tab stops further polling. Screenshot inspected (mocked update numbers75/100 and25/100); these are fixture values, not production counts. Local server stopped.
- Deployment: Windows NAS backed up and code/hash verified by Deploy-WindowsNas.ps1 (timestamp in tool output); appsettings/web.config and persistent data preserved. HTTPS health returned ok; served JS hash matches source. No Linux runtime deploy, ThumbService deploy, cache purge or Git push. Authenticated production UI and NAS cancellation timings not directly tested.
- Remaining semantics: background requires enabled configured workers; failing small files block medium background by design, but never block foreground medium. Index totals only include discovered images. Query frequency bounded and shared, but full-library NAS query latency/CPU not benchmarked in this session.
