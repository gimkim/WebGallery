# Indexing progress and date-sort follow-up

- Date: 2026-09-17 19:23 Asia/Bangkok.
- Requests: Date Taken asc/desc appears identical on paginated folders; check current DB first; show indexing progress in Management.
- Evidence: controller already sorts full folder before500-row pagination. Date ties/unknowns previously always used ascending filename. Fixed tie-break direction while keeping directories first and unknown dates last. Added1001-item regression tests. DateTakenIndexer now prioritizes recently viewed folder metadata through bounded256-entry in-memory requests, still one reader. Gallery displays whole-folder known capture-date coverage when sorted by taken; no synchronous EXIF reads.
- Live DB diagnostic: direct SMB SQLite failed disk I/O; SSH unavailable and WinRM could not authenticate. Did not change remote transport/security settings. Captured source DB+WAL into ignored App_Data/date-audit-20260917-191846, verified source/copy DB SHA256 with shared-read stream and local SQLite quick_check ok. Snapshot19:18:48:149349 images,407 dates,148875 pending,67 completed without date,0 retry. Earlier backup173 figure was explicitly corrected. First audit copy failed validation and was not used. No source DB mutations or production re-index triggered.
- Added Admin-only no-store IndexingProgress API with10s shared cached aggregate/query gate, counting current-owner indexed images only. Completed-with-date/no-date/pending/retry partition the total. Re-index retained dates are not counted complete until reread. Includes live folder scanner status, without inventing a folder-total percentage. Management bar/labels update every15s while visible, prevent overlap, use scoped fetch/timers, retain stale values with error message. No disk scan for reporting.
- Changed GalleryIndexService, DateTakenIndexer/DI, FileSystemService sorting, Gallery controller/viewmodel/view, Admin controller/view, site CSS/JS, AGENTS, DateTakenHarness and new indexing-progress-browser test.
- Validation: build/publish Windows-IIS and Linux-Docker passed. DateTakenHarness passed counters, re-index, unknown/tied direction and1001-item global sorting checks. Local ASP.NET/headless Edge passed Management API/category totals/bar rendering, guest denial, date dropdown directions/SPA/cookie. JS syntax and git diff --check passed. No authenticated NAS browser or full NAS folder reproduction; metadata-priority timing not benchmarked. Local runtime stopped.
- NAS application deployed through preserving helper, hash-verified, config retained; pinned HTTPS health checked. Linux output published, container not deployed. Existing caches/keys/DB preserved, no migration reintroduced, no Git push.
