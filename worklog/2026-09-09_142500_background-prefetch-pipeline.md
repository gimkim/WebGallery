# Continuous background supply for local and remote generation

2026-09-09 14:25 Asia/Bangkok.

- Request: recurring CPU gaps resemble batches; continuously refill work on both execution hosts while respecting on-demand priority.
- Findings: existing feed has no fixed batch completion barrier, but queries next page only when current page is exhausted, and each configured worker also performs filesystem validation and SQLite finalization before requesting another job. Medium workers repeatedly query global runnable-small state; medium generation and feed both write ready state.
- Changes: Services/IndexedThumbnailFeed.cs prefetches exactly one bounded64-entry page, excluding all current-page/in-flight identities and observing pending task on disposal. BackgroundThumbnailService prepares/finalizes up to2x configured workers (max32), leaving actual decode concurrency controlled by unchanged local/remote queues. GalleryIndexService caches runnable-small probes250ms with normal-write invalidation, retains live medium-page guard and avoids redundant medium-ready updates. AGENTS.md/.agents/PROJECT_NOTES.md updated.
- Validation: ThumbnailFeedHarness crosses150 entries while first remains blocked and verifies slow next-page query overlaps current-page consumption; no duplicates/lost rows and cancellation pass. ThumbnailPriorityHarness verifies1/16 generation slots refill while readiness writes are held, caps intact, foreground preemption/order/full-queue admission intact. GalleryIndexHarness passes after adapting direct-SQL test changes to bounded probe expiry. MediumThumbnailHarness passes remote/local fallback, dedup, preemption and cleanup. Both Release publish profiles pass without warnings.
- Deployment: Windows NAS application backed up/copied/hash-verified by existing helper; configs, database, keys/cache preserved. LAN-pinned HTTPS health checked. No Linux runtime or ThumbService deployment, no Git push or cache purge.
- Limits: verified deterministic supply/concurrency scenarios, not user-machine CPU utilization or sustained network/disk throughput. No promise of100% CPU; media sizes, transport and storage still affect utilization. Small eligibility can lag external/direct-SQL changes by at most250ms, normal index mutations invalidate promptly.
