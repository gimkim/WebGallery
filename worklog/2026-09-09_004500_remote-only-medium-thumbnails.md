# Remote-only medium thumbnails

- Request: approximately1500px WebP for oversized browser thumbnails and progressive full-view placeholders; on-demand only with connected remote, no local/background medium generation; existing medium cache usable offline.
- Changes: Services/ThumbnailService.cs adds bounded-size medium rendition, shared remote cache locks/publication, existing-cache-first lookup, no medium index-readiness writes and explicit unavailable result. Controllers/GalleryController.cs accepts medium=true through existing scope authorization; 204/no-store for unavailable medium. site.js adds bounded4 medium requests, visible tile density/resize upgrades, cancellation, decoded-blob swaps and small/medium/full generation guards. Tests/MediumThumbnailHarness and medium-thumbnail-browser.cjs; agent notes updated to supersede prior no-medium instructions.
- Server validation: Release build passed without warnings/errors. Harness passed offline miss/no local output/no network request, eight simultaneous requests generating exactly once, 1500x1000 WebP aspect ratio, distinct small/medium files, cached medium with no remote configured, rejected background medium calls, and remote failure without local medium fallback.
- Browser validation: real headless Edge against isolated local Gallery with delayed image responses passed small480 -> medium1500 -> full2400, late medium not replacing earlier full completion, density change upgrading visible thumbnail, no browser errors. Fixture initially had only readme.txt, so an existing screenshot was copied into ignored fixture content as a test image; no production originals changed. Real (unmocked) authorized medium endpoint generated via the live local HTTPS ThumbService and returned WebP with max dimension1500 plus immutable cache headers; anonymous request denied404. Existing full-progress and revoked-Blob snapshot regressions also passed. Local test server stopped. These UI/authenticated tests were local, not production NAS.
- Deployment: Windows-IIS publish and state-preserving Windows NAS copy/hash workflow; no ThumbService binary change required (existing resize API accepts1500). Persistent DB/cache/keys/tools and NAS configs preserved. No Linux, C:\Web or Git push. NAS backup/health result is in session tool evidence/final response. No production remote outage was induced.
