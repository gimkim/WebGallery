# Collection-first folder picker workflow

- Date/time: 2026-08-10 18:27:20 Asia/Bangkok (UTC+07:00)
- User request: Remove the add-to-collection controls from normal Gallery browsing. Require users to create/open a collection first, then launch a folder picker for that collection where existing membership is visible. Also remove the duplicate top-right Gallery link and rename the top navigation Admin label to Management.
- Concepts/rules established: Collection folder selection starts only from a collection's `Add folders` action. The dedicated picker names its target collection, browses the user's folder hierarchy, marks folders already covered by membership as Included and non-selectable, labels candidate parents that contain an included descendant, and returns to the same picker location after an add. The normal Gallery contains no collection dropdown or folder-selection checkboxes. The top-left brand is the sole Gallery navigation entry; the role-restricted admin link is labeled Management while internal Admin routes/roles remain unchanged.
- Files changed: `Controllers/CollectionsController.cs`, `Controllers/GalleryController.cs`, `ViewModels/GalleryViewModels.cs`, `Views/Collections/Index.cshtml`, `Views/Collections/SelectFolders.cshtml`, `Views/Gallery/Index.cshtml`, `Views/Shared/_Layout.cshtml`, `wwwroot/css/site.css`, `wwwroot/css/site-modern.css`, `AGENTS.md`, `.agents/PROJECT_NOTES.md`.
- Validation performed:
  - `dotnet build WebGallery.csproj -c Release` - passed with 0 warnings and 0 errors, including the new Razor view.
  - `dotnet run --project tests\\FileSystemVisibilityHarness\\FileSystemVisibilityHarness.csproj -c Release` - passed.
  - `dotnet run --project tests\\LoginAttemptLimiterHarness\\LoginAttemptLimiterHarness.csproj -c Release` - passed.
  - `node --check wwwroot\\js\\site.js` - passed.
  - `dotnet list WebGallery.csproj package --vulnerable --include-transitive` - no vulnerable packages reported.
  - Focused static assertions passed: normal Gallery has no folder/collection picker controls; the dedicated picker contains selection, existing-membership, and add controls; top navigation has no duplicate Gallery/Admin labels and does contain Management.
  - `git diff --check` - passed; only normal LF-to-CRLF warnings were reported.
- Deployment: Backed up the prior IIS deployment and SQLite database under `backup\\2026-08-10_1826_pre-collection-picker\\`; the pre-deployment database integrity check returned `ok`. Published Release output and copied it to `C:\\Web\\imagegallery` while excluding `appsettings.Development.json` from the copy and preserving external database/cache/key/content state. Source/deployed `WebGallery.dll` SHA-256 matched at `25C618B7EDB861844AF323F4C94454C68E56076CC6A5D1175F28E3F5C6DE49D7`; deployed Retro CSS matched `52048B7FF6C03CEF0F2EBB7C4E4BCD0B08DB1A2EA9D01125D1AB21231216C7A9` and Modern CSS matched `C2D4B90467C2819E70AE01B27FD7EBDEFC11D3D0B29EE637974B10DA6910EDF8`. The live Collections and picker routes returned the expected anonymous 302 and the picker followed to Sign in with HTTP 200; live Modern CSS returned HTTP 200, `app_offline.htm` is absent, and production SQLite integrity remains `ok`.
- User-visible result: Users now create/open a collection, click Add folders on that collection, and choose folders in a collection-specific Gallery-style picker that clearly shows existing coverage. Normal Gallery browsing is no longer mixed with collection-editing controls. The signed-in top menu no longer repeats Gallery and shows Management instead of Admin.
- Remaining uncertainty: No authenticated interactive browser or visual UI validation was performed because this project still avoids the unstable In-app Browser surface. The signed-in collection picker interactions, mobile layout, and both themes remain a manual visual check.
