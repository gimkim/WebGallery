# Folder drag must move the folder, not upload its thumbnail

2026-09-09, Asia/Bangkok.

- Request: prevent internal WebP thumbnail uploads when dragging folder cards; move the whole folder instead.
- Cause: directory cards were not handled by the move dragstart listener, leaving native image drag active. The receiving drop handler interpreted the image Files payload as an OS upload.
- Files: wwwroot/js/file-operations.js, Controllers/FilesController.cs, Services/UploadSessions.cs, tests/file-mutations-browser.cjs, AGENTS.md and .agents/PROJECT_NOTES.md.
- Fix: disable native image/link dragging in Gallery cards; owned directory cards use a cleared custom move payload and confirmation naming the folder/contents. Internal payloads never fall through to external upload. Server supports Directory.Move without recursive copy/delete and rejects root/self/descendant/overlapping source batches, name collisions and active upload destinations. Revalidate upload destination existence before publication. Existing share/collection paths are not rewritten; confirmation warns about old links.
- Validation: Release build and Windows-IIS/Linux-Docker publishes passed. Real headless Edge suite passed against final Windows build/local isolated fixture: drag directly from decoded folder-cover img, confirm whole-folder move, zero /Files/Start requests from the drag, destination image SHA-256 equal to original, root/self/descendant rejection, active-upload rejection followed by successful cancellation/move, plus existing external folder upload/empty-directory, retry, file move, password and permission tests. No browser JS errors. Local server stopped.
- Deployment: Windows NAS backup/copy/hash verification and LAN-pinned /gallery/health invoked; exact result/backup is recorded by session tools. Preserve NAS config/database/keys/cache/tools. No real user media moved, no Linux/ThumbService/local C:\Web deployment and no Git push. Live NAS authenticated drag behavior remains a user check; local browser regression exercises the reported drag origin.
