# Immediate folder-navigation feedback

- Date: 2026-09-08, Asia/Bangkok.
- Request: Show a new loading screen immediately on folder/Back navigation instead of leaving the old page apparently unresponsive while MVC responds.
- Implementation: new folder-navigation.js handles ordinary same-tab same-origin directory-card, Back-card and breadcrumb links. Displays an opaque viewport shell immediately with target text, density-aware Grid/List placeholders, indeterminate progress and Cancel; navigates after two animation frames with a fallback timer. Suspends existing thumbnail dispatcher during navigation, resumes on cancellation/restoration. Escape cancels. Slow response text appears after 12 seconds. Generation guards suppress stale callbacks. pagehide/pageshow clear visual state for BFCache. No fabricated percentages or SPA routing; modified/new-tab clicks, downloads and browser-toolbar Back remain native.
- Files: wwwroot/js/folder-navigation.js, wwwroot/js/site.js, wwwroot/css/site.css, Views/Shared/_Layout.cshtml, tests/folder-navigation.cjs, AGENTS.md, .agents/PROJECT_NOTES.md and this worklog. Existing unrelated work preserved.
- Validation: node syntax check passed. Mock DOM harness passed immediate display before assign, paint ordering, Ctrl-click exclusion, cancellation before/after assign, BFCache reset and stale callback suppression. Existing thumbnail-dispatcher harness passed. Windows-IIS publish passed. git diff --check passed with line-ending notices only.
- Deployment: Windows NAS only via existing backup/copy/hash helper. Backup `\\Gimkim-nas\c\Users\tatsa\web-data\backups\20260908-225801-index-deploy`. All copied application files hash-verified; appsettings.json/web.config preserved; persistent DB/cache/keys/tools not replaced. No app_offline.htm remains. LAN-pinned TLS health returned status ok; new /gallery/js/folder-navigation.js returned HTTP200.
- Remaining: no authenticated real-browser/mobile visual test or slow-network browser reproduction performed; behavior tests use mocked DOM. Overlay applies to in-page Back links, not the browser toolbar's cross-document Back action. Does not accelerate server rendering; provides immediate, cancellable feedback while waiting.
