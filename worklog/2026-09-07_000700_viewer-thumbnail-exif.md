# Viewer thumbnail Blob lifetime and EXIF

- Date: 2026-09-07 00:07 Asia/Bangkok.
- Request: fix missing thumbnails while full images load and missing EXIF in Info.
- Root cause: assignThumbnailBlob revokes its Blob URL after grid load. The grid retains decoded pixels but assigning that revoked URL to the viewer fails. Earlier mock tests only asserted src strings and missed this real browser behavior. Snapshot the decoded tile to a small canvas data URL instead; fall back to the existing thumbnail request when pixels are unavailable. Keep existing sizing, generation guards and original cache paths.
- Added authorized ImageMetadata endpoint with the same private/share file resolution as ViewFile, header-only identification, camera/lens/date/exposure/aperture/ISO/focal-length/software allowlist, no GPS/serial/private maker notes, and no-store responses. Info loads metadata only when opened, renders text safely, cancels on navigation/close, handles absent EXIF and retryable failures. One scrollable popover contains basic and EXIF fields.
- Files: GalleryController, new ImageExifMetadata service, Gallery view, site.js/site.css, viewer-placeholder test, new viewer-thumbnail-browser test and ViewerMetadataHarness; AGENTS/project notes.
- Validation: build and Windows-IIS Release publish passed. Existing asynchronous memory/HTTP-cache/network placeholder tests passed. Real headless Edge regression reproduced revoked Blob failure and verified the pixel snapshot decodes; fallback for unloaded tile passed. Metadata harness encoded JPEG EXIF then identified it and verified camera, f/2.8, 1/125 s, GPS omission, and absent EXIF.
- Deployment: NAS app backed up to web-data/backups/20260907-000622-viewer-exif. Used app_offline during copy, retried briefly locked DLL successfully, removed offline marker. NAS DLL/CSS/JS hashes match publish; appsettings and web.config match backup. No database/cache/keys replaced.
- Live checks: /gallery/health status ok; public JS SHA-256 matches publish and contains both changes; unauthenticated metadata probe returned 404. No authenticated live Gallery/EXIF end-to-end test or real user image inspection; browser regression is isolated and uses actual snapshot code.
