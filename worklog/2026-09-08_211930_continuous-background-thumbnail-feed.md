# Continuous background thumbnail scheduling

- Date: 2026-09-08, Asia/Bangkok.
- Request: Fix periodic CPU spikes/idle gaps during indexed background thumbnail generation at four workers; on-demand appeared more continuous.
- Evidence: BackgroundThumbnailService previously queried 64 items, awaited completion of that entire Parallel.ForEachAsync batch, then unconditionally waited three seconds. This introduces guaranteed idle time despite a backlog and leaves workers idle behind a slow last item. The screenshot alone does not rule out every other resource limitation, but these scheduling gaps were directly confirmed in source.
- Changes: Added IndexedThumbnailFeed with bounded async enumeration and in-flight identity tracking. Free workers continue through subsequent database pages without a full-page barrier. Each query allows room for active rows and snapshots active identities before reading to avoid reissuing a completed row from a stale query result. The existing three-second wait now occurs only after the stream drains or fails. Existing priority queue, worker settings, total concurrency, cache identity, fingerprint checks and cancellation remain unchanged. Runtime status updates as items finish rather than only at batch completion.
- Files: Services/IndexedThumbnailFeed.cs, Services/BackgroundThumbnailService.cs, tests/ThumbnailFeedHarness, AGENTS.md, .agents/PROJECT_NOTES.md, this worklog. Preserved unrelated dirty work.
- Validation: ThumbnailFeedHarness held item0 open while processing through item150 in 775 ms, then completed all200 with no duplicate scheduling and no concurrency above4; cancellation while awaiting active work passed. GalleryIndexHarness and BackgroundThumbnailHarness passed (real SQLite/image generation, watcher/fingerprint checks, priority/caps/cancellation). Windows-IIS publish passed; git diff --check passed with line-ending warnings only.
- Deployment: Windows NAS only, via deployment/Deploy-WindowsNas.ps1. Backup at `\\Gimkim-nas\c\Users\tatsa\web-data\backups\20260908-211832-index-deploy`. All application files hash-verified, appsettings.json/web.config preserved, database/cache/keys/tools not replaced. Maintenance marker removed. DLL SHA256 003DC3ADE37C5311E3F23A3389E0492272B33F18B498E97CD7B15886986D3100 matches publish.
- Live verification: TLS Host/SNI gimgim.ddns.net pinned to192.168.1.29:443; /gallery/health status ok; /gallery/Account/Login HTTP200.
- User-visible result: Software-imposed batch waits eliminated; configured free workers can refill immediately while older slow jobs remain active. No worker count or total CPU limit silently changed.
- Remaining: No live NAS CPU trace, throughput benchmark or authenticated browser test was captured. Actual CPU utilization can still vary with image sizes, decoding, I/O, SQLite work or foreground priority. The 775 ms figure is a deterministic synthetic scheduling test, not image-generation throughput or NAS performance.
